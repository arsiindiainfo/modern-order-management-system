VITE_API_BASE_URL=http://localhost:3000/api

# Leave blank to disable the login page's reCAPTCHA widget entirely.
# Must match backend/.env's RECAPTCHA_SECRET_KEY (same Google site).
VITE_RECAPTCHA_SITE_KEY=
