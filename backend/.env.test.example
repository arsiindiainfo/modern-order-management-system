NODE_ENV=test
PORT=3001

# Same container as .env, a DIFFERENT database — integration tests never
# touch the dev database (which carries seeded demo data).
DB_HOST=localhost
DB_PORT=14330
DB_USERNAME=sa
DB_PASSWORD=Str0ng!LocalDevPass
DB_NAME=OrderManagementSystemTest

DB_TRUST_SERVER_CERTIFICATE=true

JWT_SECRET=replace-with-a-random-32-plus-character-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_DAYS=7
